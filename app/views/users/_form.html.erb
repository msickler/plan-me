<div class="row uniform 50%">
  <div class="6u 12u$(xsmall)">
		<%= form_for user do |f| %>

			<%= f.label :name %>
			<%= f.text_field :name, placeholder: (user.errors.full_messages_for(:name).first || "Name") %>

			<%= f.label :email %>
			<%= f.text_field :email, placeholder: (user.errors.full_messages_for(:email).first || "Email") %>

			<%= f.label :password %>
			<%= f.password_field :password, placeholder: (user.errors.full_messages_for(:password).first || "A good one" ) %>

      <%= f.label :role %>
      <%= f.collection_select :role, User::ROLES, :to_s, :humanize %>

			<%= f.label :personality, "What is your travel personality?" %>
			<%= f.select :personality, [['Adventurous'], ['Romantic'], ['Intellectual']] %>

			<%= f.label :reason, "What is the reason for this trip?" %>
			<%= f.select :reason, [['Honeymoon'], ['Family Trip'], ['Cleansing Time'], ['For Fun'], ['For Tourism']] %>

			<%= f.label :budget, "What is your budget?" %>
			<%= f.select :budget, [['$1,000'], ['$5,000'], ['$7,000'], ['$10,000']] %>

      <%= f.label :international, "Willing to travel international?" %>
			<%= f.select :international, [['Yes'], ['No']] %>

			<%= f.label :companion, "How many people are you bringing?" %>
			<%= f.select :companion, [['1-2'], ['2-3'], ['3-4'], ['5+']] %>
			<br><br>
				<%= f.submit "submit" %>
		<% end %>
	</div>
</div>
