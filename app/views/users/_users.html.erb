<tbody>
  <tr>
    <% users.each do |user| %>
      <td><%= user.role %></td>
      <td><%= link_to user.name, user_path(user) %></td>
      <td><%= user.personality%></td>
      <td><%= user.reason %></td>
      <td><%= user.international %></td>
  </tr>
</tbody>
<% end %>
