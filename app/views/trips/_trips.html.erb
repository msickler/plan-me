<tbody>
  <tr>
    <% trips.each do |trip| %>
      <td class="tooltip">
        <% if current_user.is_admin? %>
          <%= link_to '⌦', trip, method: :delete, data: { confirm: 'Are you sure?' } %>
          <%= link_to trip.name.upcase, trip_path(trip) %>
          <%= link_to "(Edit)", edit_trip_path(trip) %>
        <% else %>
          <%= link_to trip.name.upcase, trip_path(trip) %><span class="tooltiptext">✈</span>
        <% end %>
      </td>
      <td><%= link_to trip.user.name, user_path(trip.user)%></td>
      <td><% trip.categories.each do |category| %>
        <%= link_to category.name, trip_category_path(trip, category) if trip.category_ids %> |
        <% end %>
      </td>
  </tr>
</tbody>
<% end %>
