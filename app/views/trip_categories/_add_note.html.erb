<%= form_for @trip do |f| %>

    <%= hidden_field_tag "category_id", @category_id %>
      <%= hidden_field_tag "trip_id", @trip.id %>
      <%= hidden_field_tag "trip", @trip.id %>
       <%= fields_for @trip_category do |tc| %>
       <%= tc.label :note %>
       <%= tc.text_field :note %><br />
     <% end %>
     <%= f.submit %>
   <% end %>
